{% extends 'base.html.twig' %}

{% block body %}
    <div class="container p-t-5 p-b-2">


        {#<div class="col-md-6">#}
            {#<div class="btn-white-lg p-l-1">#}
                {#NAJNOWSZE<span style="color: #303030;">MEMY</span>#}
            {#</div>#}
            {#<img class="img-fluid center-block video p-b-0"#}
                 {#src="{{ asset('assets/mem/') ~ meme.url }}" alt="{{ meme.title }}">#}
            {#<div class="row p-y-0 m-x-0 grey">#}
                {#<div class="col-xs-1 p-x-0">#}
                    {#<img class="img-fluid pull-xl-left p-b-0 grey" src="https://www.gravatar.com/avatar/23?s=48">#}
                {#</div>#}
                {#<div class="col-xs-10 p-l-0">#}
                    {#<p class="text-xs-left  m-b-0" style="padding-top: 0.3rem;">#}
                        {#<a class="font-weight-bold" href="{{ path('meme.id', {'id': meme.id}) }}" style="color: #000000;font-size: 0.9rem; text-decoration: none;">Zobacz komentarze!</a></p>#}
                {#</div>#}
                {#<div class="col-xs-1 p-x-0">#}
                    {#<img class="img-fluid pull-xs-right p-b-0 grey" src="{{ asset('assets/img/cs_50.png') }}">#}
                {#</div>#}
            {#</div>#}
            {#<div class="btn-white p-l-1 p-y-06">{{ meme.title }}</div>#}
            {#<div class="btn-green text-xs-center m-b-1">#}
                {#<a href="{{ path('meme') }}">Pokaż więcej</a>#}
            {#</div>#}
        {#</div>#}


        <div class="row">
            <div class="col-md-8">
                {% if meme %}
                    {% for mem in meme %}
                        <div class="meme">
                            <div class="m-title">
                                <a class="text-center" href="{{ path('meme.id',{'id': mem.id}) }}">
                                    <h3 class="m-b-0">
                                        {{ mem.title|slice(0,50) }}
                                    </h3>
                                </a>
                            </div>
                            <div class="m-img">

                                <a href="{{ path('meme.id',{'id': mem.id}) }}">
                                    <img src="{{ asset('assets/mem/') ~ mem.url }}" alt="{{ mem.title|slice(0,50) }}" class="img-fluid pull-xs-left video p-b-0">
                                </a>
                            </div>
                            <div class="m-bottom">
                                <div class="fb-like" data-href="{{ path('meme.id',{'id': mem.id}) }}"
                                     data-layout="standard" data-action="like" data-show-faces="false"
                                     data-share="true">
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                    <div class="row">
                        <div class="col-md-6">
                            {% if page > 0 %}
                                <a href="{{ path('meme',{'page': page-1}) }}" class="btn btn-raised btn-danger btn-block">Poprzednie</a>
                            {% endif %}
                        </div>
                        <div class="col-md-6">
                            <a href="{{ path('meme',{'page': page+1}) }}"
                               class="btn btn-raised btn-danger btn-block">Kolejne</a>
                        </div>
                    </div>
                {% else %}
                    <h1>Brak memów :( Smutek.jpg</h1>
                {% endif %}
            </div>
            <div class="col-md-4">
                <a href="{{ path('meme.add') }}" type="button" class="btn btn-primary btn-lg btn-block">Dodaj nowy</a>
                {{ include('navbar.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}