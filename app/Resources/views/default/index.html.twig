{% extends 'base.html.twig' %}

{% block content %}
    Stream <br>
    {% for vod in video %}
        {{ vod.title }} {{ vod.videoid }}<br>
    {% endfor %}

    <img src="{{ asset('assets/mem/') ~ meme.url }}" alt="{{ meme.title }}">

    {% for s in stream %}
        {{ s.name }}
    {% endfor %}

{% endblock %}

{% block javascripts %}
    {{ parent() }}
{% endblock %}
