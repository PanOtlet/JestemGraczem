{% extends 'base.html.twig' %}

{% block body %}
    <div class="container-fluid top p-t-3 p-b-2">
        <div class="container">
            <div class="col-sm-6 btn-clear-lg p-l-1 ">
                <span style="color: #8bc34a;">{{ 'button.newest.new'|trans }}</span>{{ 'button.newest.video'|trans }}
            </div>
            <div class="col-sm-6">
                <a href="{{ url('video.add') }}" type="button" class="btn btn-green pull-sm-right">
                    <span class="p-x-3">{{ 'button.add'|trans }}</span>
                </a>
            </div>
        </div>
    </div>
    <main class="container p-t-1">
        {% block adbanner %}{{ parent() }}{% endblock %}
        <div class="row">
            <div class="col-xs-8">
                <div class="row">
                    <div class="container-fluid">
                        {% if videos %}
                            <div class="row">
                                {% for video in videos %}
                                    <div class="col-md-6">
                                        <div class="card">
                                            <div class="card-block">
                                                <h4 class="card-title m-b-0">{{ video.title|slice(0,22) }}...</h4>
                                            </div>
                                            <a href="{{ url('video.id',{'id': video.id}) }}">
                                                <img src="http://img.youtube.com/vi/{{ video.videoid }}/mqdefault.jpg"
                                                     alt="{{ video.title|slice(0,23) }}"
                                                     class="img-responsive">
                                            </a>
                                        </div>
                                    </div>
                                {% endfor %}
                            </div>
                            <div class="row">
                                <div class="col-md-6">
                                    {% if page > 0 %}
                                        <a href="{{ url('video',{'page': page-1}) }}"
                                           class="btn btn-raised btn-danger btn-block">{{ 'button.prev'|trans }}</a>
                                    {% endif %}
                                </div>
                                <div class="col-md-6">
                                    <a href="{{ url('video',{'page': page+1}) }}"
                                       class="btn btn-raised btn-danger btn-block">{{ 'button.next'|trans }}</a>
                                </div>
                            </div>
                        {% else %}
                            <h1>Brak filmów :( Smutek.jpg</h1>
                        {% endif %}
                    </div>
                </div>
            </div>
            <div class="col-md-4">
                <a href="{{ url('video.wait') }}" type="button" class="btn btn-green btn-block"><span
                            class="p-x-3">{{ 'button.waitingroom'|trans }}</span></a>
                {% block sidebar %}{{ parent() }}{% endblock %}
            </div>
        </div>
    </main>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
{% endblock %}
