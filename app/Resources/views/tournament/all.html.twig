{% extends 'base.html.twig' %}

{% block body %}
    <div class="container-fluid top p-t-3 p-b-2">
        <div class="container">
            <div class="col-sm-6 btn-clear-lg p-l-1 ">
                <span style="color: #8bc34a;">Turnieje</span>
            </div>
            {#<div class="col-sm-6">#}
                {#<a href="{{ url('team.add') }}" type="button" class="btn btn-green pull-sm-right"><span class="p-x-3">Dodaj nową</span></a>#}
            {#</div>#}
        </div>
    </div>
    <div class="container">
        <div class="jumbotron t-title">
            {% for turniej in turnieje %}
                {{ turniej.name }}
            {% endfor %}
        </div>
    </div>
{% endblock %}
