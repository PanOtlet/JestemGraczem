{% extends "legacy/base.html.twig" %}

{% form_theme form ':Form:form.html.twig' %}

{% block body %}
    <div class="container-fluid top p-t-3 p-b-2">
        <div class="container">
            <div class="col-sm-6 btn-clear-lg p-l-1 ">
                <span style="color: #8bc34a;">JOIN</span>TEAM
            </div>
        </div>
    </div>
    <div class="container p-t-5 p-b-2">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}